
first_w="W"
mv $first_w"P"* fdl1-sign.bin
mv $first_w"Q"* fdl2-sign.bin
mv $first_w"Z"* u-boot-spl-16k-sign.bin
mv $first_w"U"* u-boot-sign.bin
mv $first_w"B"* boot.img
mv $first_w"H"* cache.img
mv $first_w"S"* userdata.img
mv $first_w"Y"* system.img
mv $first_w"R"* recovery.img
mv $first_w"O"* FHD_1280_720.bmp
mv $first_w"L"* fastbootlogo.bmp
mv $first_w"C"* custom.img

mv $first_w"F"* vbmeta-sign.img

mv $first_w"G"* PM_pike2_cm4.bin
mv $first_w"M"* SC9600_pike2_pubcp_modem.dat

mv $first_w"A"* PIKE2_DM_DSP.bin

mv $first_w"D"* pike2_cm4.bin

mv $first_w"I"* gnssmodem.bin
mv $first_w"W"* gnssbdmodem.bin

mv $first_w"N"* pike2_pubcp_nvitem.bin
mv $first_w"E"* persist.img
mv $first_w"V"* prodnv.img
mv $first_w"K"* proinfo.img
mv $first_w"J"* tos-sign.bin
mv $first_w"X"* sml-sign.bin
mv $first_w"T"* vendor.img




